### QuickSort

Build using the command:

```
g++ -fopenmp main.cpp ParallelQuickSort.cpp ParallelQuickSort.h SequentialQuickSort.cpp SequentialQuickSort.h -o Quicksort.exe
```

Or through the bash script provided:

```
./build.sh
```